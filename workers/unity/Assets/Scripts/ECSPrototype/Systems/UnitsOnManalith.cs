using UnityEngine;
using System.Collections;
using System.Collections.Generic;

namespace LeyLineHybridECS
{
    public class UnitsOnManalith : MonoBehaviour
    {
        /*
        public List<UnitOnCell> UnitsOnCells;
        public List<KeyValuePair<Unit, int>> UnitsKeyValueList;

        private void Start()
        {
            UnitsKeyValueList = new List<KeyValuePair<Unit, int>>(UnitsOnCells.Count);

            KeyValuePair<Unit, int> emptyPair = new KeyValuePair<Unit, int>(null, 0);

            for (int i = 0; i < UnitsOnCells.Count; i++)
                UnitsKeyValueList.Add(emptyPair);

            //Debug.Log(UnitsKeyValueList.Count);
        }
        */
    }
}

